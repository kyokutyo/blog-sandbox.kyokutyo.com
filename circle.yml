machine:
  node:
    version: 6.1.0
dependencies:
  override:
    - npm install -g firebase-tools
deployment:
  production:
    branch: master
    commands:
      - firebase deploy --token=$FIREBASE_TOKEN --non-interactive
test:
  override:
    - echo "test"
